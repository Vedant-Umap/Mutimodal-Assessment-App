export const metadata = {
  title: 'Multimodal Assessment AI',
  description: 'AI-driven candidate assessment',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
